class Solution {
public:
    int minPartitions(string n) {
        char maxDigit = '0';

        for (char c : n) {
            maxDigit = max(maxDigit, c);
        }

        return maxDigit - '0';
    }
};
